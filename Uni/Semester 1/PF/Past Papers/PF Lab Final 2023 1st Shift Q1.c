#include<stdio.h>
#include<stdlib.h>
#include<string.h>

typedef struct
{
	char name[50];
	int id;
}category;

typedef struct
{
	char name[50];
	float price;
	int id;
}product;

void AddCategory(category c)
{
	FILE *cat = fopen("Category.txt", "a");
	while(f(&c, ))
	
	
	
	fclose(cat);
}